import model from './submit.model';
import po from './submit.po';
import asset from './submit.asset';

// functions must return a promise
const all = {
  model,
  po,
  asset,
};

export default all;
